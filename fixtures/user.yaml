App\Entity\User:
    user1:
        email: test_user@example.com
        password: <hashPassword('a_weak_user_password')>
    user2:
        email: test_admin@example.com
        password: <hashPassword('a_strong_admin_password')>
        roles:
            - "ROLE_ADMIN"
